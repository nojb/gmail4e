(defconst gmail-auth-endpoint "https://accounts.google.com/o/oauth2/v2/auth"
  "Google OAuth2 authorization endpoint.")

(defconst gmail-auth-client-id "...")

(defun gmail-auth-request-code (scopes login-hint success)
  )

(defun gmail-auth-request-token (code)
  )
